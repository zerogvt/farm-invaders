{
  "name": "farm-invaders",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "esbuild tests/simulation.test.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/simulation.test.mjs && node node_modules/.cache/simulation.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
